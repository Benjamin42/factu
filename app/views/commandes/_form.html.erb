<%= simple_form_for @commande, :html => {:class => "form-horizontal"} do |f| %>
<% if notification = f.error_notification %>
<div class="alert alert-error fade in">
	<a class="close" data-dismiss="alert" href="#">&times;</a>
	<%= notification %>
</div>
<% end %>

<!-- Numéro de facture -->
<%= f.input :num_factu, :disabled => @commande.isFreeze %>

<!-- Bon de livraison -->
<%= f.association :bdl, :collection => Bdl.all, :prompt => "Choisissez un Bon de Livraison",
  :key => :id , :label_method => :form_title , :disabled => @commande.isFreeze%>

<!-- Client -->
<%= f.association :client, :collection => Client.all, :prompt => "Choisissez un client",
  :key => :id , :label_method => :form_title , :disabled => @commande.isFreeze%>

<!-- A livrer -->
<%= f.input :a_livrer, :disabled => @commande.isFreeze %>

<!-- Nombre d'etiquette -->
<%= f.input :nb_etiquette, :disabled => @commande.isFreeze %>

<!-- Date de livraison -->
<div class="control-group">
	<label class="checkbox inline" >
		<%= f.input :is_livraison %>
	</label>
	<%= f.text_field :date_livraison, :class => "datepicker hidden", :value => @commande.try(:date_livraison).try(:strftime, '%m/%d/%Y') %>
</div>

<!-- Date de paiement -->
<div class="control-group">
	<label class="checkbox inline" >
		<%= f.input :is_paiement, :label => 'Payée' %>
	</label>
	<%= f.text_field :date_paiement, :class => "datepicker hidden", :value => @commande.try(:date_paiement).try(:strftime, '%m/%d/%Y') %>

</div>

<table class="table">
	<tr>
		<th>Produit</th>
		<th>Quantité</th>
		<th>Cadeau</th>
	</tr>
	<% for comProduit in @commande.commande_produit %>
	
		<%= fields_for "commande[commande_produit_attributes][]", comProduit do |comProduit_form| %>
		<tr>
			<td><%= comProduit.produit.label %> : </td>
			<td><%= comProduit_form.text_field :qty, :disabled => @commande.isFreeze %></td>
			<td><%= comProduit_form.text_field :qty_cadeau, :disabled => @commande.isFreeze %></td>
			<td><%= comProduit_form.hidden_field :produit_id %></td>
		</tr>
		<% end %>
	<% end %>
</table>

<div class="form-actions">
	<%= button_tag(type: 'submit', class: "btn btn-primary") do %>
	<i class="icon-ok icon-white"></i> Save
	<% end %>
</div>

<% end %>
